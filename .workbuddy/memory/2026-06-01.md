# 2026-06-01 工作日志

## 新功能审查
- 审查新功能 `proto_msg_parser.py`（纯 Python varint protobuf 解析器）
- 审查 `douyin_im_sync.py` 改动：Playwright 不再 JS fetch 消息，改由 protobuf 解析；新增 `_check_playwright_ready()`
- 审查 `douyin_ws.py` 改动：ping 间隔 30s→15s；WS 消息类型过滤被移除（回归）
- Bug: CACHE_DIR 仍是相对路径（`douyin_im_sync.py:17`）

## 时间戳修复
- `proto_msg_parser.py` 统一使用 WS 的 `_decode_timestamp()` 替代固定范围检查
- 从实际 protobuf 缓存提取 10 条样本数据，发现 server_message_id 是 Snowflake 风格 ID
- Field 0x20（当前标为 index）的值为 `1672502400xxxxxx`，更具时间戳特征
- 输出诊断指南 `docs/timestamp_recovery_guide.md`

## UI 修复
- 头像缓存问题文档：`docs/avatar_and_status_issue_2026-05-31.md`
- 首页状态灯语义修正：有 cookie ≠ 已连接
