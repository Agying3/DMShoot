# 2026-06-13

## 全面项目审查
- 完成第三次全项目审查，输出报告: docs/REVIEW_2026-06-13.md
- 审查 63 个源文件 (60 Python + 3 Go)，发现 12 个问题
- 🔴 高优 3 个:
  - #1 auto_reply_enabled 完全失效（设置项无效，AI 始终回复）
  - #2 AI 上下文重复注入（每条消息在 context 中出现两次，token 翻倍）
  - #3 快手登录硬编码 venv 路径 (H:\DMShoot\.venv\Scripts\python.exe)
- 上次审查修复确认: on_connected 快手已补、显示名 map 已补、closeEvent 重复已修
- 仍然遗留: _get_conn() 竞态、send_rate_limited 绕过模块锁、_patch_imports 重复执行、debug 日志无限增长

## 技术问答文档
- 回答朋友提出的 16 个技术问题，涉及功能实现/测试质量/依赖部署/已知问题四大类
- 输出文档: docs/DMShoot_技术问答.md
- 关键发现: 无 Cookie 自动续期、无自动告警、无文件日志、Go 模块零测试、27 处裸 except:
- 僵尸依赖: aiosqlite（声明但未使用）

## B站 Bug 修复 x3
### Bug 1: 扫码登录不上
- DOM 选择器检测 `.header-avatar-wrap` 匹配不到改版后的 B站
- 改 cookie 检测 `context.cookies()` 查 SESSDATA
- 加多选择器回退 `.header-login-entry → [class*=login-btn] → text=登录`

### Bug 2: bilibili-api-python 17.4+ Credential 需要新字段
- buvid3/buvid4/dedeuserid/ac_time_value 缺失 → API 返回 -400
- 扩展: cookie_reader (6字段)、models (4新DB字段)、adapter connect (全传)、platform_connector、login_page、main_window
- 涉及 7 个文件

### Bug 3: 轮询拿不到新消息
- `fetch_session_msgs(uid, cred, 1, 0)` 永远 begin_seqno=0，只取头30条
- 新增 `_session_last_seq` 追踪每会话最大 seqno

## 三层测试体系建设 (L1+L2+L3)
- 创建 2 个新测试文件，对应三层测试策略
- **L1 单元测试** `test_rate_limiter.py`: 36 测试 — token bucket/burst/refill/线程安全/多平台隔离
- **L2+L3 集成+端到端** `test_integration.py`: 27 测试 —
  - L2: B站适配器+DB管道(5)、限流器交互(3)、多平台并发(3)、B站解析边界(6)
  - L3: 完整DM流程(5)、100条并发消息(3)、异常恢复(2)
- `test_rate_limiter.py` 全部通过 (36/36)
- `test_integration.py` 全部通过 (27/27)
- 发现新 bug: B站 `_parse_message(None)` 抛出 AttributeError 未做 None 检查
- `_sync_history` 记录 last_seq → `_poll_messages` 传 begin=last_seq 只取增量

## Go 服务骨架填充 — handleSend WS 回传
- Go handleSend: 写 DB + WS 广播 send_command → 返回 "queued"
- BroadcastMessage 类型 (new_message|send_command)
- Python: start_ws_sync() 后台线程、go_send_command Signal、MainWindow 路由到 adapter.send_message()

## Go 服务骨架填充 — handleSend WS 回传
- Go `handleSend` 从空壳变为: 写 DB (is_self=1) + WS 广播 send_command → 返回 "queued"
- 新增 `BroadcastMessage` 类型 (Type: "new_message"|"send_command")
- 新增 `PushCommand()` 函数
- Python go_bridge.py: 新增 `start_ws_sync()` 后台线程 WS、`on_send_command` 回调
- Python msg_service.py: 新增 `on_send_command` 回调透传
- SettingsDialog: 新增 `go_send_command` Signal, switch to Go 时自动启动 WS 回传
- MainWindow: 新增 `_on_go_send_command` → 路由到 adapter.send_message()
- 数据流: bridge.send() → Go /api/send → WS send_command → Python adapter → 平台 API

## 屎山率评估与测试体系建设

### 屎山率重新评估
- 用户确认快手和小红书模块已放弃 → 重新计算屎山率从 48% 降到 35%
- 可删除的废弃代码: plugins/xiaohongshu/(1292行) + plugins/kuaishou/(328行) + utils/xhs_proxy.py(161行) + capture_im.py(114行) + probe_xhs_galaxy.py(147行) = 共 2,042 行 Python + tools/(116K文件) + capture/ + docker/
- 真正的问题只有 main_window.py(936行) 这一个 God Object
- 删掉死代码后屎山率可降到 ~25%

### 新增测试文件 (3 个, 111 个测试, 全部通过)
- `test_concurrency.py`: 29 测试 — 单例/优先级调度/背压/统计/关闭/异常/常量
- `test_perf_monitor.py`: 39 测试 — Metric 环形缓冲/状态/PerfMonitor 单例/7指标字典/错误率
- `test_proto_sync.py`: 43 测试 — _read_varint/extract_messages/cache_key/JSON缓存/_parse_protobuf
- 全部纯 Python，无 PySide6 依赖，conda base 可直接运行
