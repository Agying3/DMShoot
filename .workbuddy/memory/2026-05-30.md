# 2026-05-30 工作日志

## DMShoot 项目代码审查
- 完成全项目 46 个 Python 源码文件的 bug 审查
- 输出详细报告到 `docs/bug_report_2026-05-30_2244.md`
- 共发现 14 个 bug：P0×3（数据丢失、硬编码路径、cwd敏感）、P1×5（session分裂、UI冻结、竞态）、P2×6（冗余代码、脆弱写法）
- 最严重：AI 回复会覆写通讯录用户昵称/头像（每次自动回复触发）；douyin_signer.py 路径全部硬编码

## Bug 修复验证
- 9/14 已修复：Bug 1(数据丢失)、2(硬编码路径)、3(cwd敏感)、4(session分裂)、5(UI冻结)、6(死代码参数)、7(AI竞态)、8(扫码竞态)、13(dir()检查)
- 5 个 P2 级别未修：Bug 9(_ai_tasks死代码)、10(冗余排序)、11(硬编码过滤)、12(迁移每次跑)、14(防御解析)

## 性能 & 闪退审查
- 输出报告到 `docs/perf_and_crash_report_2026-05-30_2343.md`
- 🔴 P0 闪退：QPixmap 在 QThread 中创建（contact.py, 必崩）、窗口关闭时 QTimer 回调访问已删除 widget
- 🟠 P1 性能：GlowProgressBar 60fps 永远不停、ContactList O(n²) 扫描、每条消息触发全量联系人刷新、MonitorPanel 日志无限增长
- 🟡 P2：BubbleWidget QSS 重解析、asyncio.run() 重复创建事件循环

## GPU 渲染分析
- 输出报告到 `docs/gpu_analysis_2026-05-30.md`
- 当前纯 Widgets 走 CPU（QPainter），系统支持 D3D11 RHI
- 推荐路线 B：QQuickWidget 渐进替换 ChatView/ContactList/MonitorPanel，2天工作量
- 路线 A 全量 QML 最彻底但需 2 周；路线 C WebEngine 不推荐（80MB+ DLL）
- QML ListView 自动 GPU 虚拟化渲染，直接根治列表性能瓶颈
