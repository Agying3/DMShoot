# 2026-05-31 工作日志

## 抖音假消息问题
- 根因：`douyin_im_sync.py` Playwright 历史同步无 msg_type 过滤，礼物/系统通知被当作普通消息
- `douyin_ws.py` WS 实时消息 msg_type 过滤 = 7 可能过期
- 用户已修复：im_sync 加 `msg_type != 1` 过滤，WS 扩展为 `in (1, 7)`

## 头像缓存 & 首页状态问题
- 输出文档 `docs/avatar_and_status_issue_2026-05-31.md`
- 头像缓存 key 用 session_id 而非 URL → 换头像不会更新
- 侧边栏状态灯有 cookie 就亮 ● → 用户误以为已连接
- 需修复：缓存 key 改 URL hash + negative cache；状态灯加 "待验证" 中间态
