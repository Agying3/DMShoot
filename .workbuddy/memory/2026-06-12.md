# 2026-06-12

## Bug 审查 (第二轮)
- 完成更新后代码的 Bug 审查，输出报告: docs/BUG_REPORT_2026-06-12.md
- 审查范围: 上次(6/5)后新增/变更的 ~1500 行代码 (sign.py, login.py, im_client.py, kuaishou adapter, xhs_proxy.py 等)

## P0/P1 Bug 修复
- closeEvent 重复定义 → 合并为一个
- on_connected/on_disconnected 缺快手分支 → 已补
- 多处 name/enabled map 缺 `kuaishou` → replace_all=true 全局补上
- xhs_proxy STATE_FILE 不存在崩溃 → 加 exists() 检查
- traceid 每次 spawn Node → 全局缓存
- xhs_qr_ready disconnect RuntimeWarning → 加 RuntimeError 捕获

## 快手适配器
- Playwright 扫码登录完成，自动提取 www + live 双域 Cookie（17 个）
- 自动点击登录按钮 + 自动从页面提取昵称
- Web 端不支持私信收发（与 XHS 相同根因）

## XHS IM 逆向日志
- 完整重写 docs/XHS_IM_逆向日志.md：24 种方法 + 失败原因 + 根因分析 + 可能成功路径
- 新建 MarkdownViewer 弹窗组件：启动 XHS/快手监听时弹出完整日志，支持 MD→HTML 渲染
- 安装 markdown 3.10.2 (PyPI)
- AVD ARM 尝试失败：H 盘沙箱封锁，5.7GB SDK 无法访问

## 结论
- XHS + 快手 IM token 需要 root 设备，当前环境 4 条硬件路线全封
- 代码已就绪（sign/IM client/adapter），token 到手即通
- 🔴 高优 6 个: QR 弹窗 closeEvent 覆盖(#1)、on_connected 缺快手(#2)、显示名 map 漏快手(#3)、enabled 漏快手(#4)、xhs_proxy 文件不存在 crash(#5)、xray_traceid 每请求 spawn Node(#6)
- 🟡 中危 3 个: login.py 是死代码(#7)、httpx AsyncClient 未关(#8)、sec cookies 静默吞异常(#9)
- 总体评价: 快手集成多处遗漏（4个map缺快手项），小红书新模块 login.py 写好了但没接上

## 设置页面壁纸 UI 修复
- 两个按钮（添加壁纸/删除选中）位置不对 → 移到画廊下方，与下拉框组成控制行
- "当前默认壁纸（纯色）"标签错误 → 改为"深色渐变"（默认壁纸是渐变不是纯色）
- 新增 QComboBox 下拉框 `_wp_combo` 用于快捷切换壁纸
- 新增 `_populate_wp_combo()` / `_on_wp_combo_changed()` 方法
