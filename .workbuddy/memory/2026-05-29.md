# 2026-05-29

## DMShoot 屎山率分析 + 测试补全

- 对 DMShoot 项目做了全面屎山率诊断，综合评分 **52.8%**（中等偏下）
  - 致命问题：`settings_dialog.py` L11 导入不存在的 `PROMPTS`（打开必崩）
  - 重复代码：`platforms/bilibili_adapter.py` 与 `plugins/bilibili/adapter.py` 几乎一样（~600行重复），`glow_progress_bar.py` 根目录/widgets 双份
  - 测试覆盖率原来只有 2.9%（129行 / 4407行）

- 为 DMShoot 编写了完整测试套件（`test_dmshoot.py`），从 7 个测试扩展到 **82 个测试**
  - 覆盖模块：core/message (19)、core/bus (8)、storage/models (8)、storage/database (10)、ai/backend (16)、ai/prompts (5)、config (5)、plugins (6)、utils/connector (2)、utils/cookie_reader (4)、集成 (1)
  - 82/82 全部通过，exit code 0
  - 测试风格保持跟原版一致（纯 assert，不引入 unittest/pytest 框架），用临时文件隔离数据库测试

## DMShoot GUI + Adapter + Playwright 测试补全

- 创建 `test_dmshoot_gui.py`，新增 **50 个测试**，50/50 全部通过
  - Adapter 解析 (15): _parse_message 全边缘情况（文本/图片/自己消息/时间戳毫秒/字符串/缺失/空内容/系统过滤/无效sender/JSON内容/peer_name/超长/零值/负数/None）
  - GUI 组件 (14): ReplyLogEntry(含回复/无回复/Unicode), MonitorPanel(添加/清空/多条), PlatformRuler(切换/信号), BubbleWidget(对方/自己), LogPanel(追加/颜色/清空), ChatView(加载/追加)
  - Login/Cookie (11): LoginPage 初始化/状态/连接断开/toggle/cookie清除/保存, CookieWorker 创建/信号/抖音+B站提取/空结果
  - 信号集成 (3): Bus→LogPanel, Bus ai_response, 完整消息流(adapter→bus→gui)
  - Cookie 提取 (3): 完整格式/畸形字符串/抖音Cookie
  - 边界异常 (4): 超长内容/talker_id=0/负数时间戳/sender_uid=None

- 发现生产代码 bug: `MonitorPanel.add_reply_log()` 用 `self._empty.isVisible()` 判断空状态是否清除，但未显示的 widget 上 isVisible() 返回 False，导致空状态永远不清除——加入 `DEV_NOTES.md` 待修列表

- 两个测试文件合计 **132 测试，100% 通过**

## DouyinAdapter + SDK + Signer 测试

- 创建 `test_dmshoot_douyin.py`，新增 **31 个测试**，31/31 全部通过
  - douyin_sdk (12): msToken(默认/自定义长度/随机性), trans_cookies(3对/单对/空/畸形), fake_webid(默认/自定义), splice_url(基本/编码), create_auth(缺s_v_web_id/cookie传递)
  - douyin_signer (3): 模块导入, JS wrapper缓存(含get_ab/get_req_sign/get_ree_key), _call_js mock
  - DouyinAdapter (8): 初始化, 状态跟踪, disconnect保存, replied裁剪到5000, send_message格式检查(4段/2段), connect无效凭证, BaseAdapter继承验证
  - BaseAdapter._my_name (3): 初始化, 带昵称状态, 无昵称状态
  - verify_douyin (1): user.nickname字段修复确认
  - BilibiliAdapter._my_name (1): 兼容性验证
  - 插件体系 (4): douyin注册/两个插件/创建adapter

- **三文件合计 163 测试，100% 通过**
  - `test_dmshoot.py`: 82 tests
  - `test_dmshoot_gui.py`: 50 tests
  - `test_dmshoot_douyin.py`: 31 tests

## WebSocket + web_protect 测试更新

- 更新 `test_dmshoot_douyin.py` 到 **44 个测试**（+13），44/44 全部通过
  - 新功能测试:
    - DouyinAdapter 构造器: web_protect/keys 参数/ws_receiver 初始化/conv_cache
    - _make_session_id: 完整4段/缺字段/全空
    - _ensure_conversation: 缓存命中/未命中不崩溃
    - DouyinWSReceiver: 队列 put/get/get_all/queue_size/空队列/stop 未启动
    - _WrappedWS._make_on_message: 回调生成
    - AppConfig: douyin_web_protect/douyin_keys 默认值/roundtrip

- 修复旧测试因接口变更:
  - `extract_douyin_cookies_sync()` 返回 dict (含 cookie/web_protect/keys) 不再是 string
  - `_CookieWorker` douyin 路径 emit 完整 dict 不再是 string
  - `cookie_fields` 新增 web_protect/keys 字段
  - `create_adapter` 测试包含 web_protect/keys

- **三文件合计 176 测试，100% 通过**
  - `test_dmshoot.py`: 82 — core/storage/ai/config/plugins/utils
  - `test_dmshoot_gui.py`: 50 — adapter/GUI/login/playwright/signal
  - `test_dmshoot_douyin.py`: 44 — SDK/signer/adapter/WS/web_protect
\n## 抖音连接失败修复\n\n- 根因：Playwright 扫码只在 `creator.douyin.com` 提取 cookie，缺 SDK 硬依赖的 `s_v_web_id`（只在 `www.douyin.com` 域下设置）\n- 修复：\n  1. `cookie_reader.py`: 登录成功后补访 `www.douyin.com` 获取完整 cookie（+ await asyncio.sleep(2)）\n  2. `douyin_sdk.py` `create_auth()`: 缺 `s_v_web_id` 时自动生成 `verify_{random}_{random}` 伪值兜底\n- 验证：`connect()` 返回 True, UID=7581349050324026405, 通知 API 正常返回 10 条\n- 已在 `DEV_NOTES.md` 新增第 11 节记录此问题\n- commit: `aab17ec`
