# 2026-06-20

## 图表弹窗滚动重置 — 最终方案 (13:17)

### 根因
Qt 内部机制（虚函数表外路径）驱动 QScrollArea 滚动条自动回退，无法从应用层完全阻止。

### 最终方案：消除滚动条
- 内容最大高度 ~350px，viewport ~580px，内容完全在视口内
- **`w.setFixedSize(780, 580)`** — widget 高度 = viewport，永不产生滚动条
- 无需滚动 → 滚动位置回退对用户完全不可见
- 删除所有抗回退检测代码、`_LockedScrollArea` 子类

### 完整演进
1. ❌ `setMinimumHeight` + `setWidgetResizable(False)` — 尺寸未锁定
2. ❌ `setFixedSize(800)` + `setWidgetResizable(True)` — 临界高度
3. ❌ `setFixedSize(800)` + `setWidgetResizable(False)` — widget 锁死但仍有回退
4. ❌ `scroll.ensureVisible = lambda` — Python monkey-patch 无效
5. ❌ `_LockedScrollArea` 子类 — C++ 虚函数覆盖，但 Qt 走其他路径
6. ⚠️ 检测恢复 — 有效但复杂
7. ✅ **`setFixedSize(780, 580)` 无滚动条** — 最简单、稳

---

## Git 提交倒退审计 (19:59)

### 发现
本地 `main` 分支只有 3 个提交，初始提交 cf634ca 仅含 176 文件，缺失:
- `external/DouYin_Spider/` 全部 38 文件 (Python + JS 抖音 SDK)
- `dmshoot-go/` 全部 6 文件 (Go 源码)
- `dmshoot/plugins/xiaohongshu/static/` 5 个签名 JS
- `prompts/` 7 个 AI 提示词 txt

### 根因
`main` 分支从 `test-coverage-fixes` 的压缩初始提交创建，不是从 `master` 分叉。

### 好消息
- **GitHub origin/main 完好** — e4d005e 含完整 250 文件和提交历史
- 本地工作区文件仍在磁盘上
- 修复: `git reset --hard origin/main` 即可恢复完整状态

---

## GitHub 提交审计 v2 (20:05)

### 对比基准: DMShoot-share b99bee8 → GitHub e4d005e

### 源代码完成度 ✅
- 全部 b99bee8 源文件均存在于 GitHub (175/177, 2个正确缺失: .env/msg-service.exe)
- DMShoot-share HEAD 新增4文件全部在 GitHub: message_analytics.py, CHANGELOG, index.html, screenshot
- 核心模块同步: dmshoot/core(13), gui(25), plugins(11), external/DouYin_Spider/builder(5) — 完全一致

### ⚠️ 敏感信息泄露 (应清理)
1. `.workbuddy/` 18个文件 — AI工作记忆/项目笔记
2. `compact_log.txt` — 应用调试日志
3. `dmshoot/data/avatars/` 10个PNG — 缓存用户头像
4. `dmshoot/desktop.ini` — Windows系统文件
5. `docs/douyin_api_capture.json` — 捕获API响应数据

### ❌ 应提交但缺失
- `bg.jpg` (DMShoot-share 有, GitHub 缺)
